{
  "scripts": {
    "build:discovery": "esbuild src/discovery-semantic.worker.js --bundle --format=iife --platform=browser --minify --outfile=overlay/javascripts/discovery-semantic.worker.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "wrangler": "^4.123.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@orama/orama": "^3.1.18",
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.1"
  }
}
